# script to source packages from the git 'packages' directory
library(pacman, lib.loc = 'packages')
library(raster, lib.loc = 'packages')
library(shiny, lib.loc = 'packages')
library(RColorBrewer, lib.loc = 'packages')
library(malariaAtlas, lib.loc = 'packages')
library(shinydashboard, lib.loc = 'packages')
